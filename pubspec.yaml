name: easy_rich_editor 
description: "Show your documents using widgets easily"
version: 1.0.0
homepage:

environment:
  sdk: '>=3.4.3 < 4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  meta: ^1.16.0
  collection: ^1.19.1
  flutter_quill_delta_easy_parser: ^1.1.6
  isolate_manager: ^6.0.1 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
