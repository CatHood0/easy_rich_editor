name: easy_text
description: "Easily add metadata attributes to your text"
version: 1.0.0
homepage:

environment:
  sdk: '>=3.4.3 < 4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  
  collection: ^1.18.0
  characters: ^1.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
